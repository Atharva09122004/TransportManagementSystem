# Dependencies
vendor/
composer.phar

# SQLite database (contains real user data — don't commit)
api/transport.sqlite

# Sensitive config — contains SMTP credentials
api/mail_config.php

# OS / Editor files
.DS_Store
Thumbs.db
*.swp
